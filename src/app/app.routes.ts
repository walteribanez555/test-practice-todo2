import { Routes } from '@angular/router';
import { TodosComponent } from './presentation/components/todos/todos.component';

export const routes: Routes = [
  {
    path : '',
    component :TodosComponent,
  }
];
